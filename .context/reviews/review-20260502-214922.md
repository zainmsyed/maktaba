# Code Review 2026-05-02T21:49:22Z

**Status:** complete  
**Created:** 2026-05-02T21:49:22Z  
**Completed:** 2026-05-02  
**Scope:** story  
**Story:** story-010  
**Static analysis:** fallow audit — fail (5 files scanned)  
**Focus:** story-010 completion review  
**Trigger:** complete-story

---

## Goal
Inspect the Story 010 implementation and direct integration points for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

---

## Findings
### Finding 1
- Severity: low
- Category: simplification
- Summary: `sortDocuments` remains over the configured complexity threshold and should be split into mode-specific comparators.
- Evidence: Fallow reports `frontend/src/routes/library/+page.svelte:234 — function sortDocuments exceeds complexity threshold (score: 23)`. The function currently handles title sorting, date-added sorting, and last-opened/update fallback sorting in one branch-heavy comparator.
- Recommendation: Extract helpers such as `getDocumentTitleSortKey`, `getCreatedTimestamp`, `getLastActivityTimestamp`, and a `DOCUMENT_SORT_COMPARATORS` map so each comparator has one responsibility and can be tested independently.
- Rule candidate: —

### Finding 2
- Severity: medium
- Category: simplification
- Summary: `focusHighlightById` is too complex for a fragile viewer integration path.
- Evidence: Fallow reports `frontend/src/routes/library/[documentId]/+page.svelte:686 — function focusHighlightById exceeds complexity threshold (score: 16)`. This function combines DOM lookup strategies, optional scrolling, focus/outline lifecycle, simulated click activation, and popup opening.
- Recommendation: Split it into `findHighlightElement`, `decorateFocusedHighlight`, `activateHighlightElement`, and `openHighlightPopupAfterFocus`; keep `focusHighlightById` as a small orchestration function.
- Rule candidate: When viewer locator/focus code has multiple fallbacks, isolate lookup, scroll, visual focus, and popup side effects into separate helpers so future locator fixes do not regress unrelated behavior.

### Finding 3
- Severity: medium
- Category: simplification
- Summary: `updateCurrentPageFromScroll` is over-complex in a scroll-restoration sensitive area.
- Evidence: Fallow reports `frontend/src/routes/library/[documentId]/+page.svelte:1025 — function updateCurrentPageFromScroll exceeds complexity threshold (score: 13)`. The function performs page collection, anchor calculation, visible-area scoring, current-page mutation, and status updates in one block.
- Recommendation: Extract `getViewerPages`, `getScrollAnchor`, `scoreVisiblePage`, and `setCurrentPageFromScroll` so scoring can be unit-tested and later scroll-restoration changes stay localized.
- Rule candidate: For reader scroll tracking, keep page scoring pure and separate from state mutation/status messaging to reduce layout-jump regressions.

### Finding 4
- Severity: low
- Category: simplification
- Summary: `compareNotes` exceeds complexity threshold and duplicates timestamp/page/id tie-break logic inline.
- Evidence: Fallow reports `frontend/src/routes/library/[documentId]/+page.svelte:348 — function compareNotes exceeds complexity threshold (score: 11)`. The comparator directly handles page fallback, updated/created timestamp fallback, descending timestamp order, and id tie-breaks.
- Recommendation: Extract key helpers (`getNoteSortPage`, `getNoteUpdatedTimestamp`) or convert to a tuple-style comparison helper to make the ordering easier to reason about.
- Rule candidate: —

### Finding 5
- Severity: medium
- Category: simplification
- Summary: `persistNoteDraft` is still too complex for autosave-critical logic.
- Evidence: Fallow reports `frontend/src/routes/library/[documentId]/+page.svelte:510 — function persistNoteDraft exceeds complexity threshold (score: 10)`. The function handles blank-note suppression, status transitions, create/update branching, note upsert, active editor synchronization, saved fade scheduling, and error handling.
- Recommendation: Extract `saveNoteDraft`, `syncSavedNoteToActiveEditor`, and `handleNoteSaveFailure`; keep race/revision checks explicit at the orchestration boundary.
- Rule candidate: For autosave flows, separate network persistence from active-editor state synchronization so race-condition fixes remain testable.

### Finding 6
- Severity: low
- Category: simplification
- Summary: The `highlightsStore.subscribe` callback exceeds complexity threshold.
- Evidence: Fallow reports `frontend/src/routes/library/[documentId]/+page.svelte:828 — function <arrow> exceeds complexity threshold (score: 10)`. The callback persists new local highlights, computes current store ids, and deletes backend highlights missing from the viewer store.
- Recommendation: Extract `persistPendingStoreHighlights`, `getHighlightStoreIds`, and `deleteMissingPersistedHighlights` so subscription behavior is easier to audit.
- Rule candidate: —

### Finding 7
- Severity: medium
- Category: test-gap
- Summary: The fixed global search result jump flow lacks a targeted regression test.
- Evidence: The recent bug was that clicking a result opened the PDF at saved progress rather than the search target. Current tests cover sidebar filtering (`frontend/tests/reader-page.test.ts`) and backend search, but there is no frontend test asserting that library search result clicks create the correct `?highlight=`/`?page=` URL, nor that the reader suppresses saved-page restoration and applies a query jump once after layout readiness.
- Recommendation: Add focused tests for `handleResultClick` behavior on highlight and page results, plus a reader-route test that initializes `window.location.search` with `?highlight=` or `?page=` and verifies the explicit jump overrides saved progress restoration.
- Rule candidate: When implementing cross-route jump behavior, add a regression test that covers both the source route URL construction and the destination route's restore-vs-jump precedence.

---

## Recommended Fixes
- [x] low — Split `sortDocuments` into mode-specific comparator helpers.
- [x] medium — Refactor `focusHighlightById` into lookup, focus-decoration, activation, and popup helpers.
- [x] medium — Refactor `updateCurrentPageFromScroll` to separate pure page scoring from state/status mutation.
- [x] low — Simplify `compareNotes` with extracted sort-key/tie-break helpers.
- [x] medium — Split `persistNoteDraft` into persistence and editor-state synchronization helpers.
- [x] low — Extract helper functions from the `highlightsStore.subscribe` callback.
- [x] medium — Add regression tests for global search result URL construction and reader query-jump precedence over saved progress.

---

## Other Fixes
- [x] Verify the global/library search UX with product expectations, since Story 010 asks for a global entry point but user feedback also emphasized the reader sidebar filter workflow.

---

## Completion Summary
Remediation pass completed for all recommended fixes. The flagged complexity hotspots were split into smaller helpers: library sorting now uses mode-specific comparator helpers; reader highlight focusing separates lookup/focus/activation/popup concerns; scroll page detection separates page collection/scoring from state mutation; note sorting and autosave persistence now use smaller focused helpers; and the highlights-store subscription delegates persistence/deletion work to named helpers. Added regression coverage for global search URL construction (`?highlight=` for highlight hits, `?page=` for note page hits) and for reader query-highlight jumps overriding saved reading progress. Also verified the UX decision for Story 010: keep both the global/library search entry point required by the story and the reader sidebar filter workflow requested during QA. Verification: frontend Vitest suite passes (31 tests). A follow-up `fallow audit --changed-since HEAD~1 --format compact` still reports pre-existing/changed-test-file duplication and the large `reader-page.test.ts` setup callback, but the seven review-sourced remediation items are complete.
