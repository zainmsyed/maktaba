# Remembered Rules

Manual rules captured via /remember.

**Source story:** story-009
- Rule candidate: When a rendering/viewer layer (PDF.js, highlight layers) can re-render pages during normal scrolling, also ensure scroll-restoration or programmatic scrolling runs only once after the initial layout settles — avoid re-running restores on each render to prevent layout jumps.
