# Code Review 2026-04-27T09:16:00Z

**Status:** complete  
**Created:** 2026-04-27T09:16:00Z  
**Completed:** 2026-04-27T09:16:00Z  
**Scope:** story  
**Story:** story-007  
**Focus:** Close the highlight selection/delete issues reported for story-007  
**Trigger:** manual

---

## Goal
Confirm and document the fix for the highlight selection / delete issues in the reader (story-007). Verify the cleanup of old selection helper code, routing of highlight persistence to a small route-local helper module, and proper backend delete behavior for highlights with linked notes.

## Checklist
- [x] Inspect relevant diffs and touched files
- [x] Confirm the frontend popup shows a delete control and calls the delete API
- [x] Confirm backend safely deletes highlights (and linked notes) without 500s
- [x] Add/verify unit tests for highlight helper mapping and payload generation
- [x] Update complaints log and write this review

---

## Findings
### Finding 1
- Severity: medium
- Category: regression / integration
- Summary: Deleting highlights initially returned 500 due to a child-note FK state on older databases and some leftover custom overlay code caused confusion.
- Evidence: Browser console showed 500 for DELETE /api/highlights/{id} before the backend fix; the frontend popup lacked a wired red-trash action in earlier commits.
- Recommendation: Ensure delete endpoints clean up dependent notes (or declare ON DELETE CASCADE in migrations) and surface a small route-local highlight client for clarity.
- Rule candidate: When adding a new persistence path, add a small route-local client with unit tests for mapping and payload generation.

---

## Recommended Fixes
- [x] backend: explicitly delete linked notes before deleting a highlight (to tolerate older DB states) — completed in `backend/app/main.py` (commit 49d4fc8)
- [x] frontend: wire the highlight popup to show a red delete icon and call the route-local highlight client — completed in `frontend/src/routes/library/[documentId]/+page.svelte` and `highlight-api.ts` (commit 49d4fc8)
- [x] tests: add unit tests for the highlight helper mapping/payload — `frontend/tests/highlight-api.test.ts` (commit 49d4fc8)

---

## Other Fixes
- [x] Removed obsolete custom selection helper `frontend/src/routes/library/[documentId]/highlight-selection.ts`
- [x] Updated plan and story docs to reflect the new helper and cleaner orchestration

---

## Completion Summary
Work completed:
- Frontend: Added a red delete icon to the highlight popup and delegated highlight network/mapping operations to `frontend/src/routes/library/[documentId]/highlight-api.ts`. Slimmed the reader page to orchestrate state and UI only.
- Backend: Made DELETE robust by removing linked `notes` explicitly before deleting `highlights`, preventing 500s on older DB states.
- Tests: Added `frontend/tests/highlight-api.test.ts` to verify backend → library mapping and normalized create payload generation. Stubbed `alert()` in the reader tests so the suite remains quiet.
- Docs: Updated Story 007 and the project plan to reflect the route-local helper approach. Marked the two logged complaints for story-007 as resolved in `.context/complaints-log.md` and recorded this review as the resolution artifact.

Relevant commit: 49d4fc8 (reader: extract highlight API to route-local helper; remove old selection helper; add unit tests; update docs)

Verification steps performed:
1. Manual verification in the running frontend: click a highlight, use the red trash icon, confirm DELETE network call returns 200 and highlight disappears from UI.
2. Backend logs and local testing show no 500 responses for highlight deletes after the fix.
3. Frontend unit tests pass: `highlight-api` unit tests + reader page tests.

Status: Resolved for the two complaints logged under story-007. Story 007 remains in-progress per project policy: finishing the story requires user acceptance and final review/acceptance step.


