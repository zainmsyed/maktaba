# Database
DB_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=maktaba
POSTGRES_USER=app

# Paths
DATA_DIR=/data

# API / CORS
# Set PUBLIC_API_URL to your domain + /api, or just /api if served from same origin
PUBLIC_API_URL=/api
SERVER_API_URL=http://backend:8000
CORS_ORIGINS=https://maktaba.example.com,https://www.maktaba.example.com
